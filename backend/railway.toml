[build]
builder = "nixpacks"

[deploy]
startCommand = "npm run build && node src/index.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
